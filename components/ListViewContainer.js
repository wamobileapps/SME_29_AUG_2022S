import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ListViewContainer = ({}) => {
  return (
    <View>
      <Text>ListViewContainer</Text>
    </View>
  )
}

export default ListViewContainer

const styles = StyleSheet.create({})